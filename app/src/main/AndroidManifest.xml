<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".di.RecipeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/cookbook"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecipeApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.activity.SearchRecipeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.SearchIngredientActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.ChooseIngredientActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.EditRecipeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.EditIngredientActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>